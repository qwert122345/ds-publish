# 디자인시스템 미리보기

Figma 플러그인. 현재 열린 파일의 토큰·컴포넌트·스타일을 미리볼 수 있습니다.

## 실행

```bash
cd ds-publish
npm install
npm run build
```

Figma → Plugins → Development → Import plugin from manifest... 에서 `manifest.json` 선택
